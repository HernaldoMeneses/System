#sudo apt-get update #just in case to
#sudo apt-get install openssh-server #just in case to
#sudo systemctl ssh  #just in case to
#sudo systemctl ssh  #just in case to forget this and before
#sudo systemctl status ssh  #just in case to
#sudo systemctl start ssh  #to init service
#sudo systemctl enable ssh  #to turn on
#sudo reboot  #just in case to
